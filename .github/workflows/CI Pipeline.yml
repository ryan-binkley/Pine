name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # - name: Set up {TODO}
      #   uses: actions/setup-node@v4

      # - name: Install dependencies
      #   run: TODO

      # - name: Run build
      #   run: TODO

      # - name: Run tests
      #   run: TODO
